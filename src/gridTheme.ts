import { themeQuartz } from 'ag-grid-community'

// Shared theme instance for all AG Grid tables in the app.
// Tweak with .withParams(...) if you need custom colours, spacing, etc.
export const gridTheme = themeQuartz
